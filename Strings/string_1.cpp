#include<iostream>
#include<string>
using namespace std;

int main() {
    // If we want to store string with spaces in between we can do it by using getline function
    string str;
    // getline(cin, str);
    cout << str << endl;
    char ch = '!';
    cout << int(ch) << endl; // will print ascii values

    

    return 0;
}